name: 'Test'

on: push

jobs:
  test:
    name: 'Test'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: 'Install'
        run: make install
      - name: 'Test'
        run: make test
